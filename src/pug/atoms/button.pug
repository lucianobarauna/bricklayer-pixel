mixin mx-btn(dataBtn)
  - var typeClass = "";

  case dataBtn.typeButton
    when 'success'
      - typeClass = "success";
    when 'danger'
      - typeClass = "danger";
    default
      - typeClass = "";

  a(
    class=`btn ${typeClass}`
    title=`${dataBtn.title}`
    href=`${dataBtn.url}`
  )= dataBtn.text